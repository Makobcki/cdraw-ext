[
  {
    "name": "set_fill_color",
    "description": "Set a solid fill color for an object. Pass hex_color or cmyk_color. If both colors are specified, priority is given to cmyk_color. If the document is in CMYK mode, RGB color is automatically converted to its CMYK equivalent.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Object identifier"
        },
        "hex_color": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$",
          "description": "Fill color in #RRGGBB format"
        },
        "cmyk_color": {
          "type": "object",
          "description": "Fill color in CMYK format (channel values from 0 to 100)",
          "properties": {
            "c": { "type": "number", "minimum": 0, "maximum": 100 },
            "m": { "type": "number", "minimum": 0, "maximum": 100 },
            "y": { "type": "number", "minimum": 0, "maximum": 100 },
            "k": { "type": "number", "minimum": 0, "maximum": 100 }
          },
          "required": ["c", "m", "y", "k"]
        }
      },
      "required": ["ref"],
      "anyOf": [{ "required": ["hex_color"] }, { "required": ["cmyk_color"] }]
    }
  },
  {
    "name": "remove_fill",
    "description": "Removes the solid fill color entirely (no fill). Essential for laser cutting contours.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": { "type": "string", "description": "Target shape reference." }
      },
      "required": ["ref"]
    }
  },
  {
    "name": "combine_shapes",
    "description": "Combines multiple paths into a single compound path using the even-odd fill rule. Required for shapes with inner holes (like 'O' or 'A') before cutting. Returns the ref of the resulting shape; the original refs are consumed and become invalid.",
    "parameters": {
      "type": "object",
      "properties": {
        "refs": {
          "type": "array",
          "items": { "type": "string" },
          "description": "List of shape references to combine."
        }
      },
      "required": ["refs"]
    }
  },
  {
    "name": "weld_shapes",
    "description": "Welds multiple shapes into a single outline contour, merging overlapping paths. Used to clean intersecting shapes for a single cut path. Returns the ref of the resulting shape; the original refs are consumed and become invalid.",
    "parameters": {
      "type": "object",
      "properties": {
        "refs": {
          "type": "array",
          "items": { "type": "string" },
          "description": "List of shape references to weld."
        }
      },
      "required": ["refs"]
    }
  },
  {
    "name": "simplify_curve",
    "description": "Reduces the number of nodes on a curve shape after tracing to smooth paths and improve cutting speed.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": { "type": "string", "description": "Target shape reference." },
        "tolerance": {
          "type": "number",
          "description": "Tolerance value in document units (doc.Unit). Larger means fewer nodes (default: 0.1)."
        }
      },
      "required": ["ref"]
    }
  },
  {
    "name": "set_outline",
    "description": "Set outline parameters for an object: color, thickness, and line style. To remove the outline, pass style='none'.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Object identifier"
        },
        "hex_color": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$",
          "description": "Outline color in #RRGGBB format"
        },
        "cmyk_color": {
          "type": "object",
          "description": "Outline color in CMYK format (channel values from 0 to 100)",
          "properties": {
            "c": { "type": "number", "minimum": 0, "maximum": 100 },
            "m": { "type": "number", "minimum": 0, "maximum": 100 },
            "y": { "type": "number", "minimum": 0, "maximum": 100 },
            "k": { "type": "number", "minimum": 0, "maximum": 100 }
          },
          "required": ["c", "m", "y", "k"]
        },
        "width": {
          "type": "number",
          "minimum": 0,
          "description": "Outline line thickness in document units (doc.Unit)"
        },
        "style": {
          "type": "string",
          "enum": ["none", "solid", "dash", "dot", "dash_dot"],
          "description": "Line style: no outline (none), solid, dashed (dash), dotted (dot), or dash-dotted (dash_dot)"
        }
      },
      "required": ["ref"]
    }
  },
  {
    "name": "flip",
    "description": "Flip an object horizontally or vertically relative to its center.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Object identifier"
        },
        "direction": {
          "type": "string",
          "enum": ["horizontal", "vertical"],
          "description": "Flip direction: horizontal or vertical"
        }
      },
      "required": ["ref", "direction"]
    }
  },
  {
    "name": "set_position",
    "description": "Move an object to page coordinates in document units (doc.Unit). X increases to the right, Y increases upwards from the bottom-left corner of the CorelDRAW page. By default, position is specified for the top-left corner of the object (anchor='top_left').",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Object identifier"
        },
        "x": {
          "type": "number",
          "description": "X coordinate in document units"
        },
        "y": {
          "type": "number",
          "description": "Y coordinate in document units"
        },
        "anchor": {
          "type": "string",
          "enum": [
            "top_left",
            "center",
            "bottom_left",
            "top_right",
            "bottom_right"
          ],
          "description": "Anchor point for object positioning (default: top_left)"
        }
      },
      "required": ["ref", "x", "y"]
    }
  },
  {
    "name": "set_size",
    "description": "Set the width and height of an object in document units (doc.Unit). By default, scaling occurs relative to the top-left corner without maintaining proportions.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Object identifier"
        },
        "width": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "New object width in document units"
        },
        "height": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "New object height in document units"
        },
        "keep_aspect_ratio": {
          "type": "boolean",
          "description": "Maintain aspect ratio of the object (default: false)"
        },
        "anchor": {
          "type": "string",
          "enum": [
            "top_left",
            "center",
            "bottom_left",
            "top_right",
            "bottom_right"
          ],
          "description": "Anchor point when resizing (default: top_left)"
        }
      },
      "required": ["ref", "width", "height"]
    }
  },
  {
    "name": "rotate",
    "description": "Rotate an object by a specified angle in degrees. Positive angle indicates counterclockwise rotation (CorelDRAW standard), negative indicates clockwise rotation. By default, rotation is performed relative to the center of the object.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Object identifier"
        },
        "angle": {
          "type": "number",
          "description": "Rotation angle in degrees"
        },
        "pivot": {
          "type": "string",
          "enum": [
            "center",
            "top_left",
            "bottom_left",
            "top_right",
            "bottom_right"
          ],
          "description": "Pivot point for rotation (default: center)"
        }
      },
      "required": ["ref", "angle"]
    }
  },
  {
    "name": "duplicate",
    "description": "Duplicate an object. By default, the copy is created directly on top of the original (offset_x=0, offset_y=0). Returns the ref of the new copy.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Object identifier"
        },
        "offset_x": {
          "type": "number",
          "description": "Horizontal X offset of the new copy (default: 0)"
        },
        "offset_y": {
          "type": "number",
          "description": "Vertical Y offset of the new copy (default: 0)"
        }
      },
      "required": ["ref"]
    }
  },
  {
    "name": "delete_shape",
    "description": "Delete an object from the document.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Object identifier"
        }
      },
      "required": ["ref"]
    }
  },
  {
    "name": "convert_to_curves",
    "description": "Convert a shape or text object to curves (Convert to Curves).",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Object identifier"
        }
      },
      "required": ["ref"]
    }
  },
  {
    "name": "order",
    "description": "Change the stacking order (z-order) of an object within the layer. Allows moving relative to layers (front/back/forward/backward) or relative to a specific target object (in_front_of/behind, requires passing target_ref).",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Object identifier"
        },
        "mode": {
          "type": "string",
          "enum": [
            "front",
            "back",
            "forward",
            "backward",
            "in_front_of",
            "behind"
          ],
          "description": "Movement direction or mode"
        },
        "target_ref": {
          "type": "string",
          "description": "Identifier of the target object (required when mode is in_front_of or behind)"
        }
      },
      "required": ["ref", "mode"]
    }
  },
  {
    "name": "export_svg",
    "description": "Retrieve a visual preview (PNG) and current vector SVG data of the object to analyze its content, shape, geometry, and structure.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Object identifier"
        }
      },
      "required": ["ref"]
    }
  },
  {
    "name": "import_svg",
    "description": "Create a new vector object from SVG markup. If x and y coordinates are not specified, the new object is automatically centered on the selection or page.",
    "parameters": {
      "type": "object",
      "properties": {
        "svg": {
          "type": "string",
          "description": "Full SVG markup"
        },
        "x": {
          "type": "number",
          "description": "Placement X coordinate (optional)"
        },
        "y": {
          "type": "number",
          "description": "Placement Y coordinate (optional)"
        },
        "anchor": {
          "type": "string",
          "enum": [
            "top_left",
            "center",
            "bottom_left",
            "top_right",
            "bottom_right"
          ],
          "description": "Anchor point of the new SVG when placing at (x, y) (default: top_left)"
        }
      },
      "required": ["svg"]
    }
  },
  {
    "name": "replace_shape_svg",
    "description": "Replace an existing object with a new SVG contour in place. The old object is deleted, and the new one is placed exactly in its location while preserving coordinates and stacking order (z-order). By default, the new SVG automatically scales to match the dimensions of the old object (preserve_size=true).",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Identifier of the object to replace"
        },
        "svg": {
          "type": "string",
          "description": "New vector SVG markup"
        },
        "preserve_size": {
          "type": "boolean",
          "description": "Scale the new SVG to match the dimensions (width and height) of the old object (default: true). If false, it is inserted at the original viewBox size."
        }
      },
      "required": ["ref", "svg"]
    }
  },
  {
    "name": "get_object_info",
    "description": "Retrieve detailed information about a single object. Returns: ref (string); typeName (string — common values: Rectangle, Ellipse, Curve, Text, Bitmap, Group; treat any other value as the closest matching type); x, y (top-left corner position, in doc.Unit); width, height (in doc.Unit); rotation (degrees); fill (hex_color and/or cmyk_color, or null if no fill); outline (hex_color/cmyk_color, width, style, or null if no outline); is_closed (boolean — whether a Curve-type object's path is a single closed loop; not meaningful for other types); layer (layer name); z_order (integer stacking position within the layer). For Text objects, also returns the literal text content, font_name, font_size, and alignment.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Object identifier"
        }
      },
      "required": ["ref"]
    }
  },
  {
    "name": "trace_bitmap",
    "description": "Trace (vectorize) a raster image using PowerTRACE. The result appears on top of the original and is returned as new_refs; the source bitmap is not deleted by default.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Bitmap object identifier"
        },
        "style": {
          "type": "string",
          "enum": [
            "line_art",
            "logo",
            "detailed_logo",
            "clipart",
            "low_quality_image",
            "high_quality_image",
            "technical",
            "line_drawing"
          ],
          "description": "Tracing preset (corresponds to PowerTRACE styles)"
        }
      },
      "required": ["ref", "style"]
    }
  },
  {
    "name": "get_page_info",
    "description": "Retrieve page and document context. Returns: page_width, page_height (in doc.Unit); unit (the document's current doc.Unit, e.g. 'mm', 'in', 'px' — use this to convert any planning math into the correct units before calling other tools); and shapes — the full list of objects on the active page, each with the same fields as get_object_info (ref, typeName, x, y, width, height, rotation, fill, outline, is_closed, layer, z_order, and text fields for Text objects). Call this to synchronize with the current document state before planning multi-step or multi-object operations, or after any tool failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "set_text",
    "description": "Change the content of a text block, as well as optionally its font family, size, color, and alignment. Styling parameters (font_name, font_size, hex_color, alignment) apply to the entire text block.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Text object identifier"
        },
        "text": {
          "type": "string",
          "description": "New text content"
        },
        "font_name": {
          "type": "string",
          "description": "Font name (e.g., Arial, Times New Roman)"
        },
        "font_size": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Font size in points (pt)"
        },
        "hex_color": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$",
          "description": "Text color in #RRGGBB format"
        },
        "cmyk_color": {
          "type": "object",
          "description": "Text color in CMYK format (channel values 0..100)",
          "properties": {
            "c": { "type": "number", "minimum": 0, "maximum": 100 },
            "m": { "type": "number", "minimum": 0, "maximum": 100 },
            "y": { "type": "number", "minimum": 0, "maximum": 100 },
            "k": { "type": "number", "minimum": 0, "maximum": 100 }
          },
          "required": ["c", "m", "y", "k"]
        },
        "alignment": {
          "type": "string",
          "enum": ["left", "center", "right", "justify"],
          "description": "Text alignment"
        }
      },
      "required": ["ref"]
    }
  },
  {
    "name": "group_shapes",
    "description": "Group multiple selected objects into a single group.",
    "parameters": {
      "type": "object",
      "properties": {
        "refs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of object identifiers to group"
        }
      },
      "required": ["refs"]
    }
  },
  {
    "name": "ungroup_shapes",
    "description": "Ungroup a group of objects.",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Group object identifier"
        }
      },
      "required": ["ref"]
    }
  },
  {
    "name": "align_objects",
    "description": "Align objects relative to each other or to the page.",
    "parameters": {
      "type": "object",
      "properties": {
        "refs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of object identifiers to align"
        },
        "align_h": {
          "type": "string",
          "enum": ["left", "center", "right", "none"],
          "description": "Horizontal alignment: left, center, right"
        },
        "align_v": {
          "type": "string",
          "enum": ["top", "center", "bottom", "none"],
          "description": "Vertical alignment: top, center, bottom"
        },
        "relative_to": {
          "type": "string",
          "enum": ["selection", "page"],
          "description": "Align relative to selection or page (default: selection)"
        }
      },
      "required": ["refs"]
    }
  },
  {
    "name": "distribute_objects",
    "description": "Evenly distribute selected objects horizontally or vertically.",
    "parameters": {
      "type": "object",
      "properties": {
        "refs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of object identifiers to distribute"
        },
        "direction": {
          "type": "string",
          "enum": ["horizontal", "vertical"],
          "description": "Distribution direction: horizontal or vertical"
        },
        "mode": {
          "type": "string",
          "enum": ["equal_spaces", "equal_centers"],
          "description": "Distribution mode: equal spacing between edges (equal_spaces) or between centers (equal_centers)"
        }
      },
      "required": ["refs", "direction"]
    }
  }
]
